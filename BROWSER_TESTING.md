# 独立浏览器测试指南

构建过程不会自动打开浏览器。请在准备进行人工验收时，手动执行以下步骤。

## 1. 启动构建产物

二选一：

- 运行安装包 `release-browser/Tyrano-Translator-Browser-0.3.1-Setup.exe`，安装后从开始菜单启动；
- 直接运行 `release-browser/win-unpacked/Tyrano Translator Browser.exe`。

当前构建没有 Windows 代码签名证书，SmartScreen 可能显示“未知发布者”。请先核对安装包 SHA-256：

```text
E7621AC10BA0556F146154F5BE778E8B8451E0BD1A16B3ECDF8ACD9E3C7F61D7
```

## 2. 配置翻译

1. 点击工具栏中的“翻译设置”。
2. 填写自己的 API Key；不要把 Key 发到聊天、截图或提交到 Git。
3. 保持默认模型、简体中文和“纯译文”模式，保存设置。
4. 在地址栏输入作品编号，例如 `3092`，浏览器会转到相应作品页。

## 3. 验收纯译文模式

进入游戏并推进剧情，确认：

- 日文出现前会等待 AI，随后直接显示中文，不先闪出日文；
- 逐字显示、点击继续、自动播放等 Tyrano 行为仍然可用；
- 中文沿用游戏字号和样式，缺字时由系统中文字体回退；
- 背景、立绘、按钮等图片完全不变；
- 再次遇到相同原文时能直接使用缓存。

## 4. 验收 Ruby 对照模式

1. 打开翻译设置，将模式改为“Ruby 对照”。
2. 分别选择译文在原文上方和下方，各推进几句剧情。
3. 确认原文保留、每行译文位置正确、中文可读，并检查窄消息框是否发生拥挤或遮挡。

## 5. 验收故障恢复与持久化

- 临时填入一个无效 Key，确认请求失败时会显示错误提示并最终恢复原文，不会永久卡死剧情；完成后立即恢复正确 Key。
- 关闭并重新打开应用，确认设置仍然存在。
- 在设置页分别清空缓存和术语表，确认操作有反馈且游戏仍可继续翻译。
- 尝试输入站外地址，确认浏览器拒绝离开 `novelgame.jp` 及其子域。

## 6. 反馈测试结果

记录作品编号、出现问题的原文、所选模式、预期结果和实际结果。截图前请确保画面中没有 API Key。若问题只在特定剧情发生，也请记录可复现的存档位置或操作顺序。
