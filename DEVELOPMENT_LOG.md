# Tyrano Translator 开发记录

本文件用于记录每个可验收开发步骤。当前阶段仅保留本地记录，不创建 Pull Request。

## 提交规范

- `add:` 新增文件或能力
- `fix:` 修改、修复现有实现
- `feat:` 其他功能性工作

## 记录格式

每一步应包含：

- 日期与步骤编号
- 本步目标
- 实际改动
- 验证方式与结果
- 遗留问题或下一步

## Step 000 — 建立开发记录与确认约束

- 日期：2026-09-22
- 状态：完成
- 目标：建立本地开发记录，并固定项目范围和协作约束。
- 实际改动：新增 `DEVELOPMENT_LOG.md`。
- 已确认需求：
  - 构建用于浏览器内 Tyrano 引擎游戏的实时文本翻译扩展。
  - 实现思路可参考 Julixian 的 GPP，但不要求复制其实现。
  - 翻译与字典能力均由 AI 提供，玩家只需配置自己的 API Key。
  - 不翻译图片中的文字。
  - 当前阶段不创建 PR，每完成一步均在本文件追加记录。
  - 未经明确要求或必要性说明，不启动浏览器。
- 验证：确认文件已写入仓库，且记录包含当前全部约束。
- 下一步：确定最小可行架构并创建浏览器扩展项目骨架。

## Step 001 — 建立本地 Git 仓库基线

- 日期：2026-09-22
- 状态：完成
- 目标：把当前目录整理为具有初始提交的本地 Git 仓库。
- 实际改动：将默认分支从 `master` 调整为 `main`，并把开发记录纳入初始提交。
- 验证：检查当前分支、提交记录和工作区状态。
- 下一步：确定最小可行架构并创建浏览器扩展项目骨架。

## Step 002 — 连接 GitHub 远程仓库

- 日期：2026-09-22
- 状态：完成
- 目标：把本地仓库连接到用户现有的 GitHub 仓库。
- 实际改动：
  - 连接公开仓库 `ShiroRin-87/Tyrano-Translator`，远程名称为 `origin`。
  - 保留远程已有的 `README.md` 和提交历史，本地提交线性衔接在远程 `main` 之后。
  - 将本地初始提交规范化为 `add: initialize repository`。
  - 记录后续提交前缀规范。
- 验证：`main` 已正确跟随远程历史，未覆盖远程内容。
- 下一步：创建浏览器扩展项目骨架。

## Step 003 — 创建 Manifest V3 扩展骨架

- 日期：2026-09-22
- 状态：完成
- 目标：建立无需构建即可加载的浏览器扩展基础结构。
- 实际改动：
  - 新增 Manifest V3 清单、后台 Service Worker、内容脚本、弹窗和设置页入口。
  - 内容脚本仅在发现 `#tyrano_base` 或 `.tyrano_base` 时标记 Tyrano 页面。
  - 新增 Node.js 静态检查脚本，验证清单版本和所有入口文件是否存在。
  - README 增加项目范围和本地检查命令。
- 设计依据：TyranoScript 官方源码以 `#tyrano_base.tyrano_base` 作为游戏根节点，消息内容位于消息层中；后续使用 DOM 观察方式适配多个 Tyrano 版本，不修改游戏脚本。
- 验证：运行 `npm run check`、`npm test` 以及 JavaScript 语法检查。
- 下一步：实现 Tyrano 文本捕获、去抖、去重和翻译结果写回。

## Step 004 — 实现 Tyrano 文本捕获层

- 日期：2026-09-22
- 状态：完成
- 目标：稳定捕获 Tyrano 逐字显示的剧情文本，并为异步 AI 翻译准备写回通道。
- 实际改动：
  - 监听标准消息层、角色名、气泡和 vchat 文本节点。
  - 对逐字 DOM 更新进行 180ms 去抖，避免每出现一个字符就调用一次 API。
  - 使用请求版本号丢弃过期响应，使用原文标记避免重复翻译。
  - 译文以独立文本节点追加，不替换原文、不扫描 `img`、不做 OCR。
  - 支持游戏根节点延迟创建和 iframe 内运行。
  - 新增文本规范化及过滤测试。
- 验证：运行清单检查、Node.js 测试、JavaScript 语法检查和 `git diff --check`。
- 下一步：在后台实现 AI 翻译、自动术语字典和结果缓存。

## Step 005 — 接入 AI 翻译、AI 术语表与缓存

- 日期：2026-09-22
- 状态：完成
- 目标：让捕获的文本通过玩家自有 API Key 完成 AI 翻译，并自动维护每个游戏的术语一致性。
- 实际改动：
  - 后台使用 Responses API 和严格 JSON Schema，一次返回译文及 AI 识别的术语更新。
  - 默认模型采用面向低成本高频任务的 `gpt-5.6-luna`，接口地址和模型均可配置。
  - 请求设置 `store: false`，API Key 只从扩展本地存储读取，不写入日志或页面。
  - 每个游戏目录维护独立 AI 术语表；同名术语由后续 AI 结果更新，最多保留 200 条。
  - 译文缓存最多保留 500 条；相同原文、模型和目标语言直接命中缓存。
  - 合并同时发生的相同请求，并向页面返回可理解的错误信息。
- 依据：OpenAI 官方 Responses API 支持用 `instructions` 和 `input` 生成文本，并通过 `text.format` 使用结构化输出；`store: false` 可避免默认存储响应。
- 验证：使用模拟 Responses API 数据测试请求结构、响应解析、术语合并、缓存匹配和游戏隔离逻辑；未调用真实 API。
- 下一步：实现 API Key、模型、接口地址、目标语言和启停设置界面。

## Step 006 — 完成玩家设置与本地数据管理界面

- 日期：2026-09-22
- 状态：完成
- 目标：让玩家无需修改代码，只需填写自己的 API Key 即可使用翻译。
- 实际改动：
  - 设置页支持启停、API Key、目标语言、模型和 Responses API 基础地址。
  - API Key 使用密码输入框并只保存到 `chrome.storage.local`。
  - 设置页显示本地译文缓存和各游戏 AI 术语数量，并支持分别清空。
  - 扩展弹窗增加实时启停开关和配置状态提示。
  - 增加响应式深色界面，不依赖外部字体、脚本或图片资源。
- 验证：运行清单检查、全部单元测试、JavaScript 语法检查和差异格式检查；未启动浏览器。
- 下一步：完善运行状态、重试与限流，补齐安装及使用文档。

## Step 007 — 引擎级提前翻译与双显示模式

- 日期：2026-09-22
- 状态：完成
- 目标：在日文进入消息层之前取得译文，并提供纯译文与 Ruby 对照两种模式。
- 实际改动：
  - 分析并记录 Tyrano 的 `nextOrder → text.start → showMessage → addChars` 执行链。
  - 在页面主执行环境挂钩标签注册源及 `master_tag.text.start`，AI 返回前暂停本次文本绘制。
  - 纯译文模式以译文替换 `pm.val` 后交回引擎，日文不进入消息层。
  - Ruby 模式保留 Tyrano 原文 DOM，并以原生 `ruby/rb/rt` 包装当次文本；支持上方和下方。
  - 请求失败或 45 秒超时自动回退原文，且不自行调用 `nextOrder()`。
  - DOM 观察保留为非标准 Tyrano 版本的降级路径；引擎挂钩成功后避免重复翻译消息层。
  - 设置页增加显示模式与 Ruby 位置选项，并新增架构文档。
- 验证：运行清单入口检查、单元测试、全部 JavaScript 语法检查和差异格式检查；未启动浏览器。
- 下一步：增加请求队列、限流、重试和页面运行状态反馈，再完成打包前审计。

## Step 008 — 增加有界重试与页内错误反馈

- 日期：2026-09-22
- 状态：完成
- 目标：短暂网络故障不直接中断翻译，同时让玩家能看见可操作的配置错误。
- 实际改动：
  - 对 HTTP 408、429 和 5xx 以及临时网络异常最多重试一次，并尊重有限的 `Retry-After`。
  - 单次请求设置 25 秒超时，引擎拦截设置 60 秒总兜底；最终失败仍交回原文。
  - 缺少 API Key、鉴权或接口错误会在游戏页显示 6 秒提示，相同错误不重复刷屏。
  - 暂停翻译属于正常状态，不显示错误提示。
- 验证：新增限流后重试成功的模拟测试；运行全部测试、清单检查、语法检查和差异格式检查。
- 下一步：完成安装、权限、隐私和故障排查文档，并生成可加载的发布目录。

## Step 009 — 完成交付文档与可复现构建

- 日期：2026-09-22
- 状态：完成
- 目标：使玩家和开发者可以在不猜测配置的情况下安装、使用、排错和构建扩展。
- 实际改动：
  - README 补齐功能、双模式、安装、设置、权限、限制和故障排查说明。
  - 新增隐私说明，明确本地数据、发送给 API 的内容、不收集的内容及删除方式。
  - 新增 `npm run build`，安全清理项目内固定输出目录并生成可加载的 `dist/tyrano-translator`。
  - 版本提升至 `0.2.0`，静态检查确保 Manifest 与 package 版本一致。
- 验证：依次运行 `npm run check`、`npm test`、JavaScript 语法检查、差异格式检查和 `npm run build`，再核对构建目录内容。
- 下一步：需要在用户允许时，用实际目标网站和玩家 API Key 进行浏览器端集成验证。

## Step 010 — 发布前静态安全审计

- 日期：2026-09-22
- 状态：完成
- 目标：检查 AI 输出进入 Tyrano 原生 HTML 字符构造流程时的边界条件。
- 实际改动：
  - 纯译文中的尖括号在交给 Tyrano 前转换为视觉等价的全角字符，避免被解释为消息 HTML。
  - Ruby 模式继续通过 `textContent` 写入译文，不允许 AI 输出成为 HTML。
  - 构建脚本不再硬编码版本号，自动核对源 Manifest 与构建产物版本。
  - 发布版本更新为 `0.2.1`。
- 验证：新增恶意尖括号译文模拟测试，重新执行完整检查和构建。
- 下一步：静态实现已完成；浏览器端实际网站集成验证需等待用户明确允许启动浏览器并提供目标页面。

## Step 011 — novelgame.jp 实例兼容与字体继承

- 日期：2026-09-22
- 状态：完成
- 目标：针对实际目标站确认游戏承载方式、旧版 Tyrano 执行链和字体兼容策略。
- 实际改动：
  - 静态检查作品 `3092`，确认播放入口跳转到 `3092.sv02.novelgame.jp/game/3092/Game/`。
  - 确认实例的旧版引擎仍使用 `master_tag.text.start`、`.message_inner`、`.current_span` 和逐字 `pchar` 流程。
  - 内容脚本运行范围收紧到 `novelgame.jp` 和 `*.novelgame.jp`，保留 `all_frames` 以覆盖站内 iframe。
  - Ruby 显式继承游戏当前字体族、粗细和斜体；纯译文继续由游戏原生字体系统绘制。
  - 新增目标站兼容性文档，记录跳转、引擎版本差异、字体规则和未进行浏览器实测的边界。
  - 发布版本更新为 `0.2.2`。
- 验证：运行版本/清单/入口检查、全部测试、JavaScript 语法检查、差异格式检查并重新生成发布目录。
- 下一步：需要用户明确允许后，才在浏览器中对 `novelgame.jp` 实例进行视觉和交互验证。

## Step 012 — 增加中文字体回退

- 日期：2026-09-22
- 状态：完成
- 目标：保留游戏字体风格，同时保证译文中的简体中文字形可显示。
- 实际改动：
  - 从每次消息节点读取实际游戏字体，并在后面追加跨平台中文系统字体栈。
  - 回退顺序包含苹方、微软雅黑、Noto Sans CJK SC、思源黑体和文泉驿。
  - 纯译文、Ruby 译文和 DOM 降级译文统一使用相同回退规则。
  - 不下载远程字体，不打包大型字体文件；字号、粗细、斜体、描边和阴影继续由游戏控制。
  - 发布版本更新为 `0.2.3`。
- 验证：增加游戏字体优先、中文字体随后回退的模拟测试，并重新执行完整检查和构建。
- 下一步：等待浏览器视觉验证许可，确认目标游戏所用字体与中文回退的实际排版。
