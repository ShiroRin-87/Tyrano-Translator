{
  "name": "tyrano-translator",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "check": "node scripts/check-extension.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
