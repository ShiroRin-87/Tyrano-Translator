{
  "name": "tyrano-translator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-extension.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

