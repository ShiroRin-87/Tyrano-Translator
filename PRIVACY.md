# 隐私说明

## 本地保存的数据

扩展使用浏览器的 `chrome.storage.local` 保存数据；独立浏览器使用其用户数据目录中的 `translator-data.json` 保存同类数据，并请求仅当前系统用户可读写的文件权限：

- 玩家填写的 API Key、API 地址、模型、目标语言和显示选项；
- 已翻译的原文与译文缓存；
- AI 为每个游戏目录自动生成的术语表。

这些数据不会通过浏览器同步功能同步。本地存储不等同于密码保险箱；能够访问当前系统账户或浏览器用户配置文件的人或程序可能读取其中的数据。

## 发送到 AI 服务的数据

翻译时，扩展或独立浏览器会把以下内容发送到玩家配置的 API 服务：

- 当前捕获的文本；
- 当前游戏已生成的术语表；
- 目标语言及翻译指令。

默认 OpenAI 请求设置 `store: false`。第三方兼容服务如何处理请求数据取决于其自己的隐私政策。

## 不收集的内容

扩展不执行 OCR、不上传图片、不读取表单密码，也没有项目自有的遥测或分析服务。API Key 只用于请求玩家配置的 API 地址，不会写入游戏页面、日志或 Git 仓库。

## 删除数据

设置页可以分别清空译文缓存和 AI 术语表。移除扩展会由浏览器删除该扩展的本地存储；卸载独立浏览器前可清除设置，或手动删除其应用数据目录。如需撤销 API Key，还应前往对应 API 服务商的控制台操作。
